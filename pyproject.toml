[project]
name = "quantlab"
version = "0.1.0"
description = "Open-source quantitative research and backtesting framework"
requires-python = ">=3.12"

dependencies = [
    "pandas",
    "yfinance",
    "matplotlib",
    "numpy",
    "pytest",
    "ruff",
    "black"
]